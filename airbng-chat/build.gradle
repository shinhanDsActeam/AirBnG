bootJar.enabled = false
jar.enabled = true
dependencies {
    implementation project(':airbng-common')
    implementation project(':airbng-platform')
}
tasks.named('test') {
    ignoreFailures = true
}