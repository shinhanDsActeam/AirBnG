package com.airbng.core.repository;

import com.airbng.core.domain.jimtype.LockerJimType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LockerJimTypeRepository extends JpaRepository<LockerJimType, Long> {
}
