package com.airbng.admin.repository;

import com.airbng.admin.domain.PendingLockerJimtype;
import org.springframework.data.jpa.repository.JpaRepository;

import java.awt.*;

public interface LockerReviewJimTypeRepository extends JpaRepository<PendingLockerJimtype, Long> {
}
