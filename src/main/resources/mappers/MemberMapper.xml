<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.airbng.mappers.MemberMapper">
    <insert id="insertMember" parameterType="com.airbng.domain.Member">
        INSERT INTO Member (email, name, phone, nickname, password, status, profile_image_id, created_at, updated_at)
        VALUES (#{email}, #{name}, #{phone}, #{nickname}, #{password}, #{status}, #{profileImage.imageId}, NOW(), NOW())
    </insert>
</mapper>
