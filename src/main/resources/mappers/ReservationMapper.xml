<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.airbng.mappers.ReservationMapper">


    <resultMap id="ReservationDetails" type="com.airbng.domain.Reservation">
        <result property="reservationId" column="reservation_id"/>
        <result property="reservationState" column="reservation_state"/>
        <result property="startTime" column="start_time"/>
    </resultMap>

    <select id="findReservationById" parameterType="Long" resultMap="ReservationDetails">
        SELECT *
        FROM reservation
        WHERE reservation_id = #{reservationId}
    </select>

    <update id="updateReservationState" parameterType="map">
        UPDATE reservation
        SET state = #{state}
        WHERE reservation_id = #{reservationId}
    </update>

</mapper>